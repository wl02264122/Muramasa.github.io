# Set the default behavior to LF
* text=auto

# Explicitly declare text files we want to be normalized and converted to native line endings on checkout.
*.md text eol=lf
*.html text eol=lf
*.css text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
